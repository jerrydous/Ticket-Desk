1. 项目的第一段提示词：请你做一个迷你工单台，这应该是一个很小的本地 Web 服务，模拟「派工单、写备注、标记完成」，不用登录，也不用做前端。用 Docker Compose 起两个容器就行：api：你的服务（Python FastAPI 或 Node Express，选你顺手的）db：PostgreSQL16数据请放进 Postgres
2. 一些强制性的约束要写进rule里，cursor每次执行修改前会自动按照上面的约束自己的实现，防止cursor随手把.env提交到git上等情况发生
3. Cursor写的自动测试在跑完之后数据会留在数据库里，污染了数据库，当时我是在跑完测试后运行查看工单的指令发现的；发现后我通过提示词（我注意到到一个问题，你写的自动测试跑完后测试用的数据会留在数据库里，你不应该把测试用的数据跑完以后清除吗）引导cursor进行了修改
4. 我主要检查了主程序和README.md, 保证核心的逻辑是正确的和能让其他人很快的上手。然后我写的东西主要是在Ticket-dest.mdc，尽量完善对cursor的约束，保证说明不产生歧义。
5. 按照README.md里的命令就可以跑通

